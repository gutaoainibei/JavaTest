<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    
    <!--
        development :开发模式
        work： 工作模式
        -->
        <!-- 配置别名，对model包进行配置,这里默认是类名 -->
    <typeAliases>
      <package name="MybatisTest2.model"/>
    </typeAliases>
    <properties resource="db.properties"></properties>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${driver}" />
                <property name="url" value="${driver}" />
                <property name="username" value="${driver}" />
                <property name="password" value="${driver}" />
            </dataSource>
        </environment>
    </environments>
    <mappers>
       <mapper resource="MybatisTest2/xml/StudentMapper.xml"/>
      <!--   <mapper class="mybatisTest.mapper.UserMapper"/>-->
    </mappers>
</configuration>