<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    
    <!--
        development :开发模式
        work： 工作模式
        -->
        <!-- 配置别名，对model包进行配置,这里默认是类名 -->
    
    <properties resource="db.properties"></properties>
    <typeAliases>
      <package name="MybatisTest2.model"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${driver}" />
                <property name="url" value="${url}" />
                <property name="username" value="${username}" />
                <property name="password" value="${password}" />
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!--<mapper resource="MybatisTest2/xml/StudentMapper.xml"/>-->
       <mapper resource="MybatisTest2/xml/ClassesMapper.xml"/>
         <!-- <mapper resource="MybatisTest2/xml/TeacherMapper.xml"/>-->
      <!--   <mapper class="mybatisTest.mapper.UserMapper"/>-->
    </mappers>
</configuration>